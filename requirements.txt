# ==============================================================================
# Dépendances ESSENTIELLES : sans elles, au moins un des 10 projets plante
# au démarrage (imports non protégés par try/except dans le code).
# ==============================================================================
streamlit>=1.30

# NLP / ML de base (utilisés par plusieurs projets : NER, vectorisation,
# topic modeling, sentiment, classification, plagiat, résumé, chatbot,
# tendances, recommandation)
scikit-learn>=1.3
numpy>=1.24
scipy>=1.11
pandas>=2.0
matplotlib>=3.7

# Scraping (projet 2, et scrapers réutilisables dans d'autres projets)
beautifulsoup4>=4.12
requests>=2.31

# spaCy + modèle français : requis par le projet 1 (NER), utilisé en
# lemmatisation optionnelle (fallback automatique sinon) dans les
# projets 2, 3, 4, 5, 9.
# IMPORTANT : le nom de package PyPI "fr-core-news-sm" N'EXISTE PAS.
# Les modèles spaCy s'installent via l'URL directe du wheel (méthode
# officielle documentée par spaCy), pas comme un paquet PyPI classique.
spacy>=3.8,<3.9
https://github.com/explosion/spacy-models/releases/download/fr_core_news_sm-3.8.0/fr_core_news_sm-3.8.0-py3-none-any.whl

# ==============================================================================
# Dépendances OPTIONNELLES (fonctionnalités avancées) : le code de chaque
# projet les importe dans un try/except et affiche un message propre
# ("non disponible dans cet environnement") si absentes. Elles ne sont
# PAS nécessaires pour que les 10 apps démarrent et fonctionnent.
#
# ATTENTION : décommenter transformers/torch/sentence-transformers/bertopic
# alourdit ÉNORMÉMENT l'installation (plusieurs centaines de Mo à plus d'1 Go)
# et peut dépasser les limites de temps/mémoire du plan gratuit de
# Streamlit Community Cloud. À n'activer que si tu as vraiment besoin des
# fonctionnalités BERT / T5 / mBART / BERTopic en démo live.
# ==============================================================================
# gensim>=4.3                 # Word2Vec (projets 2, 5)
# sentence-transformers>=2.2  # embeddings BERT (projets 2, 5)
# transformers>=4.38          # CamemBERT/FlauBERT, T5, mBART (projets 4, 7)
# torch>=2.2                  # requis par transformers/sentence-transformers
# sentencepiece>=0.2          # tokenizer T5/mBART (projet 7)
# datasets>=2.18              # fine-tuning (projet 4)
# bertopic>=0.16              # topic modeling par embeddings (projet 3)
# wordcloud>=1.9              # nuage de mots visuel (projet 9, sinon fallback en barres)
